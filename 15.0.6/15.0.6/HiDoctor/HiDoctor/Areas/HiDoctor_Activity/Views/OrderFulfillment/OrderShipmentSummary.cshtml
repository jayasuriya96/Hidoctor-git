@{
    string IsResponsive = Convert.ToString(HttpContext.Current.Session["IsResponsive"]);
    switch (IsResponsive)
    {
        case "Yes":
            Layout = "~/Views/Shared/_ResponsiveMobileLayout.cshtml";
            break;
        default:
            Layout = "~/Views/Shared/_CCMCommonLayout.cshtml";
            break;
    }
}
<style>
    .ui-widget.ui-widget-content {
        height: 200px !important;
        overflow-y: auto;
    }

   

    .modal-backdrop {
        background-color: transparent !important;
    }

    .modal-dialog {
        max-width: 850px !important;
    }

    .modal-content {
        border: none;
    }

    label {
        font-weight: 500 !important;
    }

    .col-form-label {
        font-size: small !important;
    }

    table {
        font-size: 10px !important;
    }
    #footer {
            box-shadow: none !important;
    }
</style>
<div class="startcss">
    <div class="col-sm-12">
        <form>
            <div class="form-row clearfix">
                <div class="form-group col-md-6">
                    <label for="inputEmail4">Region Name :</label>
                    <div id="regions">
                        <select class="form-control form-control-sm" id="region" ></select>
                    </div>
                </div>
            </div>
        </form>
    </div>
    <div class="col-sm-12">
        <div class="alert alert-primary text-center" role="alert" id="alert" style="display:none;">
            No Order Shipment.
        </div>
    </div>
    <div class="col-sm-12">
        <div class="card-columns" id="shipmentOrderSummary">
        </div>
    </div>
</div>
    <div class="modal" id="DetailsModal">
        <div class="modal-dialog">
            <div class="modal-content">

                <!-- Modal Header -->
                <div class="modal-header" style="padding:0px">
                    <h4 class="modal-title">Details</h4>
                    <button type="button" class="close" data-dismiss="modal">&times;</button>
                </div>

                <!-- Modal body -->
                <div class="modal-body" id="modelbody" style="height:450px;overflow-y: auto; overflow-x: auto;">

                </div>
            </div>
        </div>
    </div>
<link href="~/Areas/HiDoctor_Activity/Content/OrderFulfillment.css" rel="stylesheet" />
    <script src="~/Areas/HiDoctor_Activity/Scripts/HD/OrderShipmentSummary.js?v=@HiDoctor.BuildNumber.BuildNo"></script>
    <script src="~/Areas/HiDoctor_Activity/Scripts/CoreREST.js"></script>
    <script>
        var subDomainName=@Html.Raw(Json.Encode(ViewBag.subDomainName));
        var RegionCode=@Html.Raw(Json.Encode(ViewBag.RegionCode));
        var CompanyCode=@Html.Raw(Json.Encode(ViewBag.CompanyCode));
        var UserCode=@Html.Raw(Json.Encode(ViewBag.UserCode));
        var Company_Id=@Html.Raw(Json.Encode(ViewBag.Company_Id));
        $(document).ready(function () {
            $('#dvAjaxLoad').hide();
            AjaxGlobalHandler.Initiate();
            OrderShipmentSummary.init();
        });
    </script>
